import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const StyledBox = styled(Box)({
  color: "azure",
  height: "100%",
  overflowY: "auto",
  overflowX: "hidden",
  padding: "2%",
  width: "2000px",
});
